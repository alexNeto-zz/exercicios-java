package lista_2;

public class Vendas {

	private int[] id;
	private double totalPreco = 0;
	private int totalItens = 0;
	private double[] porcentagem;

	public Vendas() {
		porcentagem = new double[5];
		id = new int[5];
		for (int i = 0; i < id.length; i++) {
			this.id[i] = 0;
		}
	}

	public void compraRealizada(int id, double preco) {
		this.id[id] += 1;
		System.out.println(id[0] + " " + id[1] + " " + id[2] + " " + id[3] + " " + id[4]);
		this.totalItens += 1;
		this.totalPreco += preco;
		porcentagem(id);
	}

	public void porcentagem(int id) {
		for (int i = 0; i < 5; i++) {
			porcentagem[id] = (100 * this.id[id]) / this.totalItens;
		}
	}

	public double getTotalPreco() {
		return totalPreco;
	}

	public double[] getProcentagem() {
		return porcentagem;
	}
}
