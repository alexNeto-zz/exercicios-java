package lista_i;

public class Exercicio_1 {
	
	/*
		Desenvolva um aplicativo que determinará se um cliente de uma loja de departamentos
		excedeu o limite de crédito em uma conta-corrente. Para cada cliente, os seguintes
		fatos estão disponíveis: número da conta, saldo no início do mês, total de todos os
		itens cobrados desse cliente no mês, total de créditos aplicados ao cliente no mês
		e limite de crédito autorizado. O programa deve inserir todos esses fatos com ponteiros,
		calcular o novo saldo (= saldo inicial + despesas - créditos), exibir o novo saldo e
		determinar se o novo saldo excede o limite de crédito do cliente. Avisar se o
		crédito do cliente está excedido ou não.
	*/
	
	java.util.Scanner input = new java.util.Scanner(System.in);
	
	System.out.println("Digite um número: ");
	int numero1 = input.nextInt();
	System.out.println("Digite outro número: ");
	int numero2 = input.nextInt();
	
	System.out.println((numeros1 % numero2 == 0) ? "é divisivel" : "não é divisivel");
}
